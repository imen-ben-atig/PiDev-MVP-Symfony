<table class="table table-hover" id="dataTable" width="100%" cellspacing="0">
  <br>
  <br>
  <br>
    <thead>
        <a class="btn btn-primary" href="{{path('addblog')}}" >AJOUTER BLOG</a>
        <br>
        <br>
        <br>
        
      <tr>
        <th>Title</th>
        <th>Content</th>
        <th>image</th>
        <th>edit</th>
        <th>delete</th>
      </tr>
    </thead>
    <tbody>
      {% for blog in blog %}
      <tr>
        <td>{{ blog.title }}</td>
        <td>{{ blog.content }}</td>
         <td>{{ blog.createdAt |date('Y-m-d')}}</td>
         
        <td><img style="width:250x;height:100px;" src="{{ asset('uploads/' ~ blog.image) }}" alt="{{ blog.image }}" class="img-thumbnail"></td>
        <td> <a class="btn btn-primary" href="{{path('updateblog',{ id:blog.id})}}" > modifier BLOG </a> </td>
        <td> <a class="btn btn-primary" href="{{path('deleteblog',{ id:blog.id})}}" > delete BLOG </a> </td>
        
      </tr>
      {% endfor %}
    </tbody>
  </table>